name: Director read-after-write CI (temporary)

on:
  push:
    branches: [hotfix/director-read-after-write]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        working-directory: processes/company_director_enrichment
        run: python -m pip install --disable-pip-version-check -r requirements.txt
      - name: Run tests
        working-directory: processes/company_director_enrichment
        env:
          PYTHONPATH: ${{ github.workspace }}/processes/company_director_enrichment:${{ github.workspace }}/processes/eqazyna_leads
        run: python -m pytest -q tests
      - name: Compile hotfix
        working-directory: processes/company_director_enrichment
        env:
          PYTHONPATH: ${{ github.workspace }}/processes/company_director_enrichment:${{ github.workspace }}/processes/eqazyna_leads
        run: |
          python -m py_compile director_frozen_plan_hotfix.py enrich_missing_directors_v5.py
          python enrich_missing_directors_v5.py --help >/dev/null
