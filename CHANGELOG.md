## 2026-09-14 — поток 30: подразделения Астаны

- адрес Астаны распределяется по дочерним отделам продаж `65` и `66`, а не ищет пользователей только в родительском управлении `46`;
- РОПами Астаны считаются пользователи с ролью `РОП` внутри отделов `65/66` (в текущей структуре — ID `72` и `73`);
- руководитель родительского управления `46` не включается в пул продаж;
- старое закрепление сотрудника из отдела `46` переводится на наименее загруженного РОПа дочерних отделов;
- добавлены регрессионные тесты для пустого родительского отдела, распределения по менеджерам и overflow между двумя РОПами.

## 2026-09-11 — поток 33: остатки 16/18/38

- пакеты больше не зависают при большинстве/ничьей группы 16/18/38;
- если остальных связанных лидов больше, пакет распределяется на РОП 72/73;
- иначе остатки пакета переходят тому из 16/18/38, у кого больше связанных лидов;
- при точной ничьей между защищёнными менеджерами используется меньший ID как детерминированный tie-break;
- стадии лидов не меняются.

# Changelog

## 2026-09-11

- удалён завершённый блок переноса CRM из облака (`01`, `10–13`, `cloud_export`, `cloud_to_box`);
- workflow переназначения пакетов полностью пересобран без repair-потоков и без изменения стадий лидов;
- пакет без учредителя обрабатывается как `компания + лиды`, а без компании — как отдельный лид;
- прежнее правило сохранения действующего владельца удалено: все допустимые пакеты перераспределяются между РОП 72/73;
- из Талдыкоргана защищаются только пакеты, связанные с менеджерами 16/18/38; остальные пакеты участвуют в распределении;
- добавлена защита лида `401`;
- добавлены read-after-write, финальная проверка и один встроенный повтор только расхождений;
- устранён двойной вывод summary;
- e-Qazyna status sync перенумерован в workflow `34`, чтобы не было двух workflow `33`.
