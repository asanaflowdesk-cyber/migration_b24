## 2026-09-10 — v7: возврат к рабочей модели Python на self-hosted Windows

- За эталон взят архив, в котором Windows self-hosted runner уже работал с локально установленным Python 3.12.
- `scripts/prepare-python.cmd` восстановлен на прямой путь `C:\\Users\\Alyona.Sachyova\\AppData\\Local\\Programs\\Python\\Python312\\python.exe` и только создаёт локальную `.venv`.
- Из всех Windows self-hosted workflow удалён `actions/setup-python`; из потока 30 удалён PowerShell bootstrap.
- `scripts/bootstrap-python.ps1` удалён: workflow больше не скачивает и не устанавливает Python на runner.
- Добавлена регрессионная проверка: Windows self-hosted workflow не может снова получить `setup-python`, PowerShell bootstrap или другой путь Python.
- GitHub-hosted Linux workflow `00-ci.yml` и `01-cloud-export.yml` не относятся к Windows runner и сохраняют собственный `actions/setup-python`.

## 2026-09-08 — Поток 32 защищён от роботов стадии NEW

- Убрана зависимость от `user.current`: ID пользователя берётся из input `moved_by_id` или repository variable `LEAD_RECOVERY_MOVED_BY_ID`; без ID поток останавливается до записи.
- При переносе в `NEW` исходный `ASSIGNED_BY_ID` передаётся явно вместе со стадией и очисткой причины провала.
- После перевода поток ждёт отработки роботов Bitrix24, перечитывает лиды и восстанавливает исходного исполнителя, если робот его сменил.
- Если поле причины провала не очистилось первым обновлением, выполняется повторная очистка через `null`.
- После ремонта выполняется второй финальный контроль стадии, исполнителя и причины; позднее вмешательство автоматизации больше не считается успешным восстановлением.
- Для потока 32 исправлено имя переменной таймаута: используется `HTTP_TIMEOUT_SECONDS`, которое читает общий `BitrixClient`.

## 2026-09-08 — Восстановление лидов очищает причину провала

- Workflow `32 | Лиды — вернуть ошибочный провал в Новые` при переносе в `NEW` дополнительно очищает поле причины провала `UF_CRM_1785508658316`.
- `ASSIGNED_BY_ID` по-прежнему не передаётся в update и проверяется после записи.
- После обновления дополнительно проверяется, что причина провала действительно очищена.

## 2026-09-07 — Новые e-Qazyna лиды всегда стартуют в «Новый»

- Полностью убрано наследование стадии от предыдущего связанного лида.
- Любая новая заявка принудительно создаётся в `NEW`; внешняя настройка `BITRIX_LEAD_STATUS_ID` больше не может отправить новый лид в другую стадию.
- Единственное исключение — последний связанный неудачный лид с причиной `Уже работает с Евразией`: новая заявка создаётся в `JUNK`.
- Для enum-поля причины распознаются текст, ID и список; при создании нового лида записывается целевой ID значения Bitrix24, а не текстовая подпись.
- Повтор той же e-Qazyna заявки по номеру по-прежнему пропускается и не сбрасывает уже существующий лид в `NEW`.

## 2026-08-27 — Дерево рассинхрона по руководителю

- Workflow `31` перестроен вокруг руководителя как источника истины: `Руководитель → Компания → Лиды`.
- Один руководитель с несколькими компаниями объединяется по ФИО в одно дерево.
- Отчет показывает шесть человекочитаемых колонок и подсвечивает ответственных, отличающихся от ответственного руководителя.
- `apply` меняет только компании; случаи с 3+ разными ответственными по лидам остаются ручными.
- Для будущих e-Qazyna заявок добавлен поиск исторического ответственного того же руководителя по другим компаниям.

## 2026-08-27 — Ответственный компании по первому лиду

- e-Qazyna parser больше не меняет `ASSIGNED_BY_ID` уже существующей компании.
- Добавлен workflow `31 | Компании — ответственный по первому лиду`.
- Сверка использует самый ранний связанный лид по `DATE_CREATE`, затем по `ID`.
- Добавлен отдельный отчет по компаниям с 3+ разными ответственными среди связанных лидов.
- В `apply` изменяется только `ASSIGNED_BY_ID` компании; лиды, контакты и сделки не изменяются.

# Changelog

## v5 — постоянный парсер e-Qazyna в том же self-hosted репозитории

- добавлен отдельный процесс `processes/eqazyna_leads`;
- добавлен workflow `30 | e-Qazyna — ГПО в лиды`;
- workflow переведен на `cmd`, как остальные потоки этого Windows runner;
- используется существующий secret `TARGET_BITRIX_WEBHOOK_URL`;
- добавлены режимы `dry_run` и `apply` с обязательным подтверждением боевого запуска;
- парсер создаёт и обновляет только лиды, один основной лид на один БИН;
- поле `UF_CRM_1785917145255` заполняется значением `ГПО недропользователя`;
- при обновлении сохраняются текущая стадия и ответственный;
- добавлено распознавание лидов, перенесённых из облака со старыми маркерами `EQAZYNA / eQazyna|номер|БИН`;
- выбранный старый лид переводится на постоянный маркер `EQAZYNA_LEAD / БИН`, чтобы последующие запуски не создавали новый дубль;
- добавлены журналы Excel/JSON и автономные тесты парсера;
- автоматическое расписание не включено.

## v4 — полный перенос облачного дампа

- зафиксирован разовый дамп облака от 05.08.2026;
- удален повторный поток экспорта облака;
- добавлен перенос компаний, контактов, реквизитов, адресов и строгих CRM-связей;
- настроено преобразование 1 243 сделок в 1 233 лида и 10 сделок;
- исключены четыре тестовых лида с перенаправлением оставшихся связей на конвертированные сделки;
- добавлен продукт `ГПО недропользователя` в лиды и сделки;
- удалено создание пользователей, подразделений, проектов и групп;
- добавлено сопоставление пользователей по email и ФИО без учета порядка слов;
- добавлен перенос 597 задач без проектов с участниками, подзадачами, чек-листами, комментариями и файлами;
- добавлен перенос 134 CRM-дел с файлами и связями;
- добавлено чтение бинарных вложений непосредственно из облака;
- добавлена поддержка классического API задач и резервных методов REST 3.0;
- добавлено чтение файлов из чатов новых карточек задач и перенос их в соответствующие комментарии;
- добавлено сохранение состояния реального импорта между запусками GitHub Actions;
- добавлены возобновляемые карты ID и детальные отчеты;
- обновлены workflow 10–13;
- добавлены 12 автономных тестов дампа, правил миграции и файлов комментариев задач.

## 2026-09-07 — ручное восстановление ошибочно закрытых лидов
- Добавлен workflow `32-restore-failed-leads.yml`.
- Выбирает только лиды, которые сейчас находятся в провальной стадии, были переведены туда указанным пользователем и имеют `MOVED_TIME` в пределах последних N дней (по умолчанию 7).
- При восстановлении меняется только стадия на `NEW`; ответственный (`ASSIGNED_BY_ID`) не передаётся в обновление и дополнительно проверяется после записи.
- Боевой режим защищён отдельным подтверждением; dry-run является режимом по умолчанию.

## 2026-09-08 — workflow 32 runner path fix
- Убран `working-directory: processes/lead_recovery` из job defaults: self-hosted Windows runner больше не пытается стартовать `cmd.exe` в несуществующем каталоге до выполнения шага.
- Workflow 32 запускается из корня репозитория и использует явные пути `processes\lead_recovery\...`.
- Добавлена ранняя проверка наличия `recover_failed_leads.py` после checkout с диагностикой содержимого репозитория.

## 2026-09-10 — self-hosted Windows Python bootstrap fix

- All Windows self-hosted workflows now run pinned `actions/setup-python` with Python 3.12 x64 before creating `.venv`.
- `scripts/prepare-python.cmd` now uses the interpreter placed on `PATH` by `setup-python` instead of assuming Python is preinstalled for the runner service account.
- Manual execution remains supported through `PYTHON_EXE`.
- Persistent `.venv` directories are removed before creating a job-local environment.

## 2026-09-10 — GPO runner Python bootstrap v4
- Workflow `30 | e-Qazyna — ГПО в лиды` no longer depends on a preinstalled Python or on `actions/setup-python` PATH propagation.
- Added `scripts/bootstrap-python.ps1`: on Windows self-hosted runner it reuses Python 3.11+ when present, otherwise downloads the signed official CPython 3.12.10 installer and installs it job-locally into `RUNNER_TEMP` without administrator rights.
- `prepare-python.cmd` has the same bootstrap as a fallback, so even an older workflow step that only calls this script can recover automatically.

## 2026-09-10 — GPO runner Python bootstrap v5
- Исправлена PowerShell parser error в `scripts/bootstrap-python.ps1`: строка ошибки больше не использует неоднозначную конструкцию `$RequiredMinor:` и формируется через оператор `-f`.
- Добавлена статическая проверка PowerShell-интерполяции в `scripts/run_quality_checks.py`, чтобы этот класс ошибки ловился до упаковки релиза.
- Workflow `30 | e-Qazyna — ГПО в лиды` и fallback `prepare-python.cmd` продолжают использовать один и тот же job-local bootstrap Python 3.12.10.

### 2026-09-10 — v6: PowerShell `$HOME` bootstrap fix

- Исправлен `scripts/bootstrap-python.ps1`: локальная переменная `$Home` переименована в `$PythonHome`. PowerShell нечувствителен к регистру, поэтому `$Home` конфликтовал со встроенной read-only переменной `$HOME` и падал уже после успешной установки Python.
- Добавлена регрессионная проверка, запрещающая повторное присваивание `$HOME` в PowerShell-скриптах.

## 2026-09-10 — e-Qazyna application status sync

- Добавлен отдельный workflow `33 | e-Qazyna — синхронизация статусов заявок`.
- Поток не создаёт лиды: он берёт существующие e-Qazyna лиды и ищет заявку в публичном реестре по точному `flDocNum`.
- `Отклонено`, `Отозвано`, `Аннулировано` переводят лид в стадию `Провал`.
- `Выдана лицензия` переводит лид в стадию `Потенциальные сделки`; поддержан существующий вариант названия `Потенциальная сделка`.
- Остальные внешние статусы не изменяют стадию лида.
- ID стадий не захардкожены: перед работой они разрешаются через `crm.status.list`; отсутствие нужной стадии блокирует запись.
- Старые e-Qazyna карточки поддерживаются через составной `ORIGIN_ID`, заголовок и комментарий. Карточки с несколькими номерами заявок не изменяются автоматически.
- Добавлены `dry_run`, подтверждение для `apply`, JSON/CSV/текстовый журнал, повторные попытки чтения e-Qazyna и fail-closed завершение при ошибках записи/чтения.
- Windows self-hosted workflow продолжает использовать только локальный Python через `scripts/prepare-python.cmd`; `setup-python`/PowerShell bootstrap не добавлялись.

## 2026-09-10 — исправление логики e-Qazyna status sync (v9)

- Исправлено ключевое правило: **1 лид = 1 заявка**.
- Номер заявки теперь берётся только из `TITLE`: всё после `№ ` считается номером заявки. `ORIGIN_ID` и `COMMENTS` больше не участвуют в определении номера.
- Добавлена подстраховка поиска e-Qazyna карточек по названию, чтобы синхронизация не зависела только от `ORIGINATOR_ID` после миграции/ручных изменений.
- При статусах e-Qazyna синхронизируется не только стадия лида, но и поле `UF_CRM_1785508658316`:
  - `Аннулировано` → `Провал` + enum `66` (`Заявка аннулирована на сайте`);
  - `Отозвано` → `Провал` + enum `67` (`Заявка отменена на сайте`);
  - `Отклонено` → `Провал` + enum `68` (`Заявка отклонена на сайте`);
  - `Выдана лицензия` → `Потенциальные сделки` + enum `69` (`По заявке уже выдана лицензия`).
- Если стадия уже правильная, но причина отсутствует или неверная, поле причины исправляется. Запись пропускается только когда совпадают и стадия, и причина.
- Тесты потока переписаны под фактическую структуру CRM и отдельно проверяют все четыре статуса и все четыре enum-ID.
